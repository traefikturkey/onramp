# Handoff: Proxmox External Config Validation Follow-up

Created: 2026-05-18 14:55:26

## Next-session focus

Continue the Proxmox external YAML config migration work from branch `feature/multi-instance-proxmox-external`, specifically Linux validation and plan completion/archive if validation passes.

## Current state

- Code/docs/tests were committed and pushed to `origin/feature/multi-instance-proxmox-external`.
- Commit: `7236f40 feat: add Proxmox external config generation`.
- The implementation plan file is **not committed** because `.specs/` is ignored.
- Active plan path in this working copy: `.specs/proxmox-external-config/plan.md`.
- The plan's execution checklist has implementation and targeted validation marked complete, but final repo-wide validation/archive gates remain blocked.

## Important references

- Plan: `.specs/proxmox-external-config/plan.md`
- Commit: `7236f40`
- Key implementation files are in the commit; inspect the commit rather than duplicating the diff here.

## What was done

- Added Proxmox-only external instance generation/migration CLI.
- Added focused tests for generator/migration behavior.
- Wired Make targets for Proxmox external migration/generation.
- Updated external-service docs and command reference.
- Removed PR #71 numbered Proxmox template blocks and shifted to generated-output model.
- Pushed all non-private code/docs/test changes.

## Validation already run on Windows/Git Bash

Passed:

```bash
cd sietch && uv run pytest tests/test_external_instances.py tests/test_traefik_hosts.py -q
git diff --check
```

Also passed fixture-safe Make smoke checks for Proxmox migrate/generate, non-Proxmox copy behavior, and unsupported-service failure.

Blocked/failed due environment or unrelated baseline issues:

- `make test` failed under Windows/Git Bash because Docker received an invalid translated working directory: `C:/Program Files/Git/app`.
- `cd sietch && uv run pytest -q` failed with unrelated baseline test failures, mostly stdout/logging assertion mismatches and one scaffold logging `extra={"message": ...}` issue under Python 3.14.
- `yamllint -c .yamllint .` failed on existing unrelated YAML files; changed Proxmox YAML was not among the failures.

## Recommended Linux continuation commands

Run from a Linux/WSL checkout where Docker paths are native:

```bash
git fetch origin
git checkout main
git pull --ff-only origin main
git checkout feature/multi-instance-proxmox-external
git pull --ff-only origin feature/multi-instance-proxmox-external

cd sietch
uv run pytest tests/test_external_instances.py tests/test_traefik_hosts.py -q
cd ..

make test
git diff --check
yamllint -c .yamllint .
```

If the plan file is available in the Linux checkout, then resume:

```bash
/do-it .specs/proxmox-external-config/plan.md
```

If the plan file is not available, copy `.specs/proxmox-external-config/plan.md` from this working copy first, or manually update the final validation/archive status based on the plan contract.

## Completion criteria

- Linux `make test` passes, or a justified plan-approved fallback validation passes.
- YAML/whitespace validation is either fully green or unrelated baseline YAML failures are documented with changed-file clean evidence.
- Plan checklist final gates F2 and F5 can be marked complete.
- Plan can then be archived per its archive rule.

## Dolos/private artifact note

Attempted to inspect Dolos freshness with:

```bash
bin/dolos status
```

Result: `bin/dolos` was not present in this checkout/session. No Dolos pack was run. This handoff is saved under `private/handoffs/` and is private/uncommitted unless the private archive workflow is intentionally refreshed elsewhere.

## Suggested skills for next session

- `git-workflow` for branch, commit, push, and status checks.
- `python` for Sietch pytest failures or generator test work.
- `shell` / `workflow-design` for Make/Docker validation issues.
- `docs` only if validation findings require documentation updates.
- `analysis-workflow` for triaging Linux validation failures.
