http:
  routers:
    proxmox:
      rule: "Host(`proxmox.{{env "HOST_DOMAIN"}}`)"
      middlewares:
        - default-headers
      entryPoints:
        - websecure
      tls: {}
      service: proxmox

    pihole:
      entryPoints:
        - websecure
      rule: "Host(`pihole.{{env "HOST_DOMAIN"}}`)"
      middlewares:
        - default-headers
        - addprefix-pihole
      tls: {}
      service: pihole

    idrac:
      entryPoints:
        - websecure
      rule: "Host(`idrac.{{env "HOST_DOMAIN"}}`)"
      middlewares:
        - idrac
      tls: {}
      service: idrac
    idrac-console:
      entryPoints:
        - "idrac" # REQUIRED for iDRAC virtual console: Create a new TCP entry point in traefik on port 5900
      rule: "Host(`idrac.{{env "HOST_DOMAIN"}}`)"
      middlewares:
        - idrac
        - https-redirectscheme
      tls: {}
      service: idrac-console

  services:
    proxmox:
      loadBalancer:
        servers:
          - url: "https://{{env "PROXMOX_ADDRESS" "127.0.0.1"}}:8006/"
        passHostHeader: true
    pihole:
      loadBalancer:
        servers:
          - url: "http://{{env "PIHOLE_ADDRESS" "127.0.0.1"}}:80"
        passHostHeader: true
    idrac:
      loadBalancer:
        servers:
          - url: "https://{{env "IDRAC_ADDRESS" "127.0.0.1"}}"
        passHostHeader: true
    idrac-console:
      loadBalancer:
        servers:
          - url: "https://{{env "IDRAC_ADDRESS" "127.0.0.1"}}:5900"
        passHostHeader: true
    
  middlewares:
    addprefix-pihole:
      addPrefix:
        prefix: "/admin"

    default-headers:
      headers:
        frameDeny: true
        browserXssFilter: true
        contentTypeNosniff: true
        forceSTSHeader: true
        stsIncludeSubdomains: true
        stsPreload: true
        stsSeconds: 15552000
        customFrameOptionsValue: SAMEORIGIN
        customRequestHeaders:
          X-Forwarded-Proto: https

    idrac:
      headers:
        frameDeny: true
        browserXssFilter: true
        forceSTSHeader: true
        stsIncludeSubdomains: true
        stsSeconds: 15552000
        customFrameOptionsValue: SAMEORIGIN
        customRequestHeaders:
          X-Forwarded-Proto: https