# description: ubuntu base image
# https://hub.docker.com/_/ubuntu

services:
  ubuntu:
    image: ubuntu
    env_file:
      - ./services-enabled/ubuntu.env
    container_name: ubuntu
    restart: on-failure
    command: sleep infinity
    volumes:
      - ${UBUNTU_TEST_VOLUME:-.}:/mnt
