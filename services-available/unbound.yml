# description: Dns resolver
# https://github.com/pascaliske/docker-unbound

services:
  unbound:
    image: ghcr.io/pascaliske/unbound:${UNBOUND_DOCKER_TAG:-latest}
    container_name: ${UNBOUND_CONTAINER_NAME:-unbound}
    restart: unless-stopped
    volumes:
      #- ./etc/unbound:/opt/unbound/etc/unbound/
      - /etc/localtime:/etc/localtime:ro
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ}
    labels:
      #- joyride.host.name=${UNBOUND_CONTAINER_NAME:-unbound}.${HOST_DOMAIN}
      - traefik.enable=false
      - com.centurylinklabs.watchtower.enable=${UNBOUND_WATCHTOWER_ENABLED:-true}
      - autoheal=true
