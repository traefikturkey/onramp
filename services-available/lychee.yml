networks:
  traefik:
    external: true

# description: Photo management and sharing platform
# https://github.com/LycheeOrg/Lychee-Docker

services:
  lychee:
    image: lscr.io/linuxserver/lychee:${LYCHEE_DOCKER_TAG:-latest}
    container_name: lychee
    restart: unless-stopped
    networks:
      - traefik
    volumes:
      - ./etc/lychee:/config # <==== this probably needs changed ====>
      - /etc/localtime:/etc/localtime:ro
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ}
    labels:
      - joyride.host.name=lychee.${HOST_DOMAIN}
      - traefik.enable=true
      - traefik.http.routers.lychee.entrypoints=websecure
      - traefik.http.routers.lychee.rule=Host(`lychee.${HOST_DOMAIN}`)
      - traefik.http.services.lychee.loadbalancer.server.port=80
      - com.centurylinklabs.watchtower.enable=${LYCHEE_WATCHTOWER_ENABLED:-true}
      - autoheal=true
