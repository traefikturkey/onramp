###############################################
# Docling Serve Configuration
# Generated from services-scaffold/docling/env.template
###############################################

# Container settings
DOCLING_DOCKER_TAG=${DOCLING_DOCKER_TAG:-latest}
DOCLING_CONTAINER_NAME=${DOCLING_CONTAINER_NAME:-docling-serve}
DOCLING_RESTART=${DOCLING_RESTART:-unless-stopped}

# Runtime tuning
DOCLING_ENABLE_UI=${DOCLING_ENABLE_UI:-1}
DOCLING_NUM_THREADS=${DOCLING_NUM_THREADS:-8}
DOCLING_GPU_COUNT=${DOCLING_GPU_COUNT:-1}

# Network / routing
DOCLING_HOST_NAME=${DOCLING_HOST_NAME:-docling-serve}
DOCLING_TRAEFIK_ENABLE=${DOCLING_TRAEFIK_ENABLE:-true}
DOCLING_WATCHTOWER_ENABLE=${DOCLING_WATCHTOWER_ENABLE:-true}
DOCLING_AUTOHEAL=${DOCLING_AUTOHEAL:-true}
