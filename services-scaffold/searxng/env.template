###############################################
# SearXNG Configuration
# Generated from services-scaffold/searxng/env.template
###############################################

# Container settings
SEARXNG_DOCKER_TAG=${SEARXNG_DOCKER_TAG:-latest}
SEARXNG_CONTAINER_NAME=${SEARXNG_CONTAINER_NAME:-searxng}
SEARXNG_RESTART=${SEARXNG_RESTART:-unless-stopped}
SEARXNG_VOLUME=${SEARXNG_VOLUME:-./etc/searxng}

# Redis settings
SEARXNG_REDIS_RESTART=${SEARXNG_REDIS_RESTART:-unless-stopped}
SEARXNG_REDIS_WATCHTOWER_ENABLE=${SEARXNG_REDIS_WATCHTOWER_ENABLE:-true}
SEARXNG_REDIS_AUTOHEAL=${SEARXNG_REDIS_AUTOHEAL:-false}

# Network / routing
SEARXNG_HOST_NAME=${SEARXNG_HOST_NAME:-searxng}
SEARXNG_TRAEFIK_ENABLE=${SEARXNG_TRAEFIK_ENABLE:-true}
SEARXNG_WATCHTOWER_ENABLED=${SEARXNG_WATCHTOWER_ENABLED:-true}
SEARXNG_AUTOHEAL=${SEARXNG_AUTOHEAL:-false}
