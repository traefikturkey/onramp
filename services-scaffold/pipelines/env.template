###############################################
# Open WebUI Pipelines Configuration
# Generated from services-scaffold/pipelines/env.template
###############################################

# Container settings
PIPELINES_DOCKER_TAG=${PIPELINES_DOCKER_TAG:-main}
PIPELINES_CONTAINER_NAME=${PIPELINES_CONTAINER_NAME:-pipelines}
PIPELINES_RESTART=${PIPELINES_RESTART:-unless-stopped}
PIPELINES_DATA_PATH=${PIPELINES_DATA_PATH:-./etc/pipelines}

# Network / routing
PIPELINES_HOST_NAME=${PIPELINES_HOST_NAME:-pipelines}
PIPELINES_TRAEFIK_ENABLE=${PIPELINES_TRAEFIK_ENABLE:-false}
PIPELINES_WATCHTOWER_ENABLE=${PIPELINES_WATCHTOWER_ENABLE:-true}
PIPELINES_AUTOHEAL=${PIPELINES_AUTOHEAL:-false}

# API key shared with Open WebUI OPENAI_API_KEYS.
# Leave blank during scaffolding to auto-generate a secure value.
PIPELINES_API_KEY=${PIPELINES_API_KEY}
