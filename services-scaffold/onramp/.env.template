################################################
#
# OnRamp Global Configuration
#
# Edit this file with: make edit-env-onramp
#
# IMPORTANT: This file is included by Make - do NOT use ${VAR} syntax
# for variables defined in this file (causes recursive reference errors).
# Use simple KEY=value format. Docker Compose handles defaults separately.
#
################################################

# Cloudflare credentials (required)
CF_API_EMAIL=
CF_DNS_API_TOKEN=

# Host identification (required)
# HOST_NAME: short hostname of this server (e.g., "homelab")
# HOST_DOMAIN: your domain managed by Cloudflare (e.g., "example.com")
HOST_NAME=
HOST_DOMAIN=

# Timezone - see: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=US/Eastern

# User/Group IDs for container permissions
# Run 'id' to find your UID/GID
PUID=1000
PGID=1000

################################################
# Traefik Settings
################################################
# DNS_CHALLENGE_API_EMAIL defaults to CF_API_EMAIL if empty
DNS_CHALLENGE_API_EMAIL=
DNS_CHALLENGE_PROVIDER=cloudflare
TRAEFIK_ACCESSLOG=true
TRAEFIK_LOG_LEVEL=DEBUG
CF_RESOLVER_WAITTIME=60

################################################
# Backup Settings (optional)
################################################
#ONRAMP_BACKUP_LOCATION_ENV=./backups
#ONRAMP_BACKUP_EXCLUSIONS=
#ONRAMP_BACKUP_INCLUSIONS=
