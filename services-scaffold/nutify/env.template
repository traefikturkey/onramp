# NUTIFY Configuration

# Secret key for Flask sessions and password encryption (REQUIRED, min 32 characters)
# A random key has been generated in etc/nutify/keys/encryption-key.txt
# Copy it here or generate your own with: python3 -c "import secrets; print(secrets.token_hex(32))"
NUTIFY_ENCRYPTION_KEY=${NUTIFY_ENCRYPTION_KEY:-CHANGE_ME_COPY_FROM_etc_nutify_keys_encryption-key_txt}
